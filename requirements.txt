# Serial Communication
pyserial>=3.5

# Configuration File Parser
configparser>=5.3.0

# Character encoding detection (optional but useful)
chardet>=5.2.0

# MRZ parser (for passport)
mrz>=1.3.0

# GUI (for simple desktop interface)
PySimpleGUI>=4.60.5

# API (if extending with FastAPI)
fastapi>=0.103.1
uvicorn>=0.23.2

# install with: pip install -r requirements.txt
